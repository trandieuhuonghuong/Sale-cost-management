{
  "name": "sales-expense-manager-update",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "rm -rf dist && mkdir dist && cp index.html dist/index.html && cp -r assets dist/assets"
  }
}
